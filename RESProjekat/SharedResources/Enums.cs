namespace SharedResources
{   

    public enum Status
    {
        BAD_FORMAT,
        REJECTED,
        SUCCESS
    }

    public enum StatusCode
    {
        BAD_FORMAT_CODE = 5000,
        SUCCESS_CODE = 2000,
        REJECTED_CODE = 3000
    }
}
